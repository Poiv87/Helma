<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>میو</title>
    <style>
        body {
            font-family: "Tahoma", sans-serif;
            text-align: center;
            background: linear-gradient(135deg, #ff9a9e, #fad0c4);
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            color: #fff;
        }

        #message {
            margin-top: 10px;
            font-size: 18px;
            font-weight: bold;
            color: yellow;
        }

        #btn {
            margin-top: 15px;
            padding: 10px 20px;
            font-size: 18px;
            color: white;
            background: #ff5e62;
            border: none;
            border-radius: 20px;
            cursor: pointer;
            transition: 0.3s;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.3);
        }

        #btn:hover {
            background: #ff1e56;
            transform: scale(1.1);
        }

        #next {
            padding: 0;
            margin: 0;
            background-color: #eb8f93;
            color: white;
            border: none;
            border-radius: 20px;
            position: absolute;
            top: 10px;
            right: 10px;
        }

        #text {
            padding: 0;
            margin: 0;
            position: absolute;
            top: 10px;
            right: 30px;
        }
    </style>
</head>

<body>
    <button id="next" onclick="nextPage()">→</button>
    <p id="text"></p>
    <h3>برای اینکه بتونی وارد صفحه بعد شی باید میوو کنی😁</h3>
    <br>
    <h5>اگی میوو کردی این جا کلیک کن 👇</h5>
    <button onclick="checkName()" id="btn">بعدی</button>
    <p id="message"></p>

    <script>
        function checkName() {
            const sentences = [
                "یکم سعی کن😄",
                "مطمعنی میوو کردی🤔",
                "یه باره دیگه!!"
            ];

            const randomIndex = Math.floor(Math.random() * sentences.length);
            document.getElementById("message").innerText = sentences[randomIndex];
        }
        function nextPage() {
            window.location.href = "https://araiesh.vercel.app/helma/firsting"
        }

        setTimeout(function () {
            document.getElementById("text").innerText = "این جا باید کلیک کنی 😂👉";
        }, 20000);

    </script>
</body>

</html>